version: 2.0
jobs:
  build:
    docker:
      - image: kaspermn33/codechan
    steps:
      - checkout
      - run:
          name: test
          command: python tests.py
      - store_test_results:
          path: build/test-results